# Core server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# Tool dependencies
ddgs>=6.0.0          # web_search
httpx>=0.27.0        # web_scrape
beautifulsoup4>=4.12 # web_scrape

# Memory system (optional — SENNI runs without these, memory endpoints return memory_unavailable)
chromadb>=0.5.0                      # vector store
sentence-transformers>=3.0.0         # all-MiniLM-L6-v2 embeddings (~90MB download on first run)

# TTS (optional — installed separately via Aurini or manually)
# kokoro
# soundfile
