{
  "identifier": "default",
  "description": "Default capability — local webview talking to localhost:8000",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "updater:default",
    "dialog:default"
  ]
}
