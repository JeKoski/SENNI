{
  "identifier": "default",
  "description": "Default capability — local webview talking to localhost:8000",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "single-instance:default",
    "updater:default",
    "dialog:default"
  ]
}
